<p id="notice"><%= notice %></p>

<h1>ブログ一覧公開保存 Blogs Index(status 2)</h1>

<table>
  <thead>
    <tr>
      <th>Index</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @blogs.each do |blog| %>
      <tr>
        <td><%= blog.title %></td>
        <td><%= blog.status %></td>
        <td><%= link_to 'Show', blog %></td>
        <td><%= link_to 'Edit', edit_blog_path(blog) %></td>
        <td><%= link_to 'Destroy', blog, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Blog', new_blog_path %>
<br><br>
<%= link_to 'Draft Index', draft_index_path  %>
<%= link_to 'Deleted Index', deleted_index_path  %>
