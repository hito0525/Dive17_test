<h1>Deleted Index(ステータス3 Deletedされたブログ一覧画面)</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @blogs.each do |blog| %>
      <tr>
        <td><%= blog.title %></td>
        <td><%= blog.status %></td>
        <td><%= link_to 'Show', blog %></td>
        <td><%= link_to 'Edit', edit_blog_path(blog) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<br>
<%= link_to ' Back to Blogs ', blogs_path %>

